# Agent & Project Guidelines

## UI Footnote Rule
- Always preserve User ID 3316199 footnote on all screens/templates.

## DevOps Configurations
- All future Ansible, Packer, and Terraform configuration files should be placed in the `~/NM-FSM-App/devops-course` directory (`devops-course/` in repository root).
- Terraform configuration files must be placed inside the `devops-course/terraform` subdirectory.
- The Ansible vault password file (`.ansible_vault_pass`) must never be committed to the repository.
